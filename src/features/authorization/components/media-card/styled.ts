import styled from '@emotion/styled';

export const MediacardRoot = styled.div({
  display: 'flex',
  backgroundColor: 'white',
  borderRadius: '16px',
  padding: 16,
  border: '1px solid',
  borderColor: '#EDEEF2',
});
