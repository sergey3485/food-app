import styled from '@emotion/styled';

export const RestaurantTagRoot = styled.div({
  display: 'flex',
  alignItems: 'center',
  justifyContent: 'center',
  padding: '6px 12px',
  backgroundColor: '#EDEEF2',
  width: 'max-content',
  borderRadius: '100px',
});
